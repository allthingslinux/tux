# Environment files
.env*
!.env.example

# Python virtual environment and caches
.venv/
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.coverage
.mypy_cache/
.ruff_cache/

# Build artifacts
build/
dist/
*.egg-info/
.eggs/

# IDE/Editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Documentation and development files
docs-build/
site/
*.md
!README.md
!LICENSE
!requirements.md

# Development configuration
.cursorrules
.editorconfig
.pre-commit-config.yaml

# Logs
*.log
logs/

# Git (now excluded since version info is passed via build args)
# This improves build performance and security
.git/
.gitignore
.gitattributes

# Docker files (prevent recursive inclusion)
Containerfile*
compose.yaml
compose.production.yaml
compose.override.yaml.example
.dockerignore

# Cache directories
.cache/

# Plugins (drop-in extensions, should be mounted as volumes at runtime, not in image)
src/tux/plugins/
tux/plugins/

# Test files and directories
tests/
test_container.sh

# Example code
examples/

# Type stubs (development-only, not needed at runtime)
typings/
*.pyi

# Coverage reports
coverage.json
coverage.xml
lcov.info
codecov.yml

# Nix development files
flake.lock
flake.nix
shell.nix

# Cloudflare Workers config (not needed for Docker)
wrangler.toml

# Documentation build config
zensical.toml

# Documentation source files
docs/

# Runtime data (should be mounted as volumes, not in image)
data/

# Cursor IDE files
.cursor/
